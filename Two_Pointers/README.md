# 🔄 双指针 (Two Pointers)

## 💡 个人见解
通常是指在序列（数组或链表）中定义两个指针（或索引），通过某种策略（通常是往中间靠拢，或者同向快慢移动）来遍历数据结构。它往往是**多层嵌套循环（暴力破解 O(n²)）的优化解法**，可以将时间复杂度降低到 O(n) 或 O(n log n)（含排序）。**【典型双指针：左右逼近，快慢指针】**

## 🎯 适用题型
- 序列（尤其是有序序列）中需要操作多个元素（如两数之和、三数之和）。
- 数组元素的移动、覆盖与删除（如移动零、删除重复项）。
- 链表相关的操作（如寻找环、寻找倒数第 K 个节点）。
- 正常暴力解法极其耗时，需要降低时间复杂度时，优先考虑双指针。

## ⚠️ 使用注意与优化敲门
1. **一定先排序！**（针对大部分数组求和类题目，排序后能利用单调性快速调整指针，但要注意排序会改变原数组索引）。
2. **判断条件的选择**：`left < right` 还是 `left <= right`，取决于题目是要求不重合还是允许重合。
3. **双指针的边界问题**：指针移动时极易越界，每次循环必须严格校验边界。
4. **多数据降维**：如果是多个数据，通常做法是**固定一个数据，操作其他数据**，把 N 数之和降解为 2 数之和。
5. **巧用单调性剪枝**：
    - 如三数之和中，排序后如果固定元素已经大于 0，则后续双指针之和不可能为 0，直接 break 结束。
    - 如最接近的三个数和中，三个最小数的和已经大于目标数时，判断后直接break；三个最大数的和已经小于目标数时，判断后continue进入下一次循环。
    - 多个数之和时，在每一个外层都可以去重
6. 去重：
    - 外层：放剪枝里，判断当前和前一个一样并且当前不是第一个，则跳过该循环
    - 内存（找到目标时）：判断当前和下一个相同并且两个指针没有越界，则指针下移
    - set去重【一般用上面两个即可】

## 🕳️ 踩过的坑与错题本
- **坑1：去重逻辑写错**。在求和的题目中，遇到符合条件的元组后，必须让 `left` 和 `right` 跳过所有重复元素，否则答案中会出现重复解。
- **坑2：指针越界**。在循环体内移动指针时，必须先检查 `left < right`，否则会导致 `IndexError`。
- **坑3：死循环**。在判断条件中，如果两个指针指向的元素相等，必须有让指针移动的逻辑，否则会陷入死循环。
- **坑4：set()是否可哈希**。可哈希的对象可以作set元素，也可以做dict的键*【可变类型不可哈希，不可变类型可哈希】*

## 📝 题目列表
*本目录下的 `.py` 文件遵循统一注释模板。*

| 题号 | 题目                                                  | 难度 | 核心考点                         |
| :--- | :---------------------------------------------------- | :--- | :------------------------------- |
| 88   | [合并两个有序数组](./0088_Merge_Sorted_Array.py)      | 简单 | 逆向双指针                       |
| 15   | [三数之和](./0015_3Sum.py)                            | 中等 | 排序+双指针+去重                 |
| 16   | [最接近的三数之和](./0016_3Sum_Closest.py)            | 中等 | 排序+双指针+剪枝                 |
| 18   | [四数之和](./0018_4Sum.py)                            | 中等 | 排序+双指针+剪枝                 |
| 26   | [删除有序数组中的重复项](./0026_Remove_Duplicates.py) | 简单 | 双指针（快慢指针）               |
| 27   | [移除元素](./0027_Remove_Element.py)                  | 简单 | 双指针                           |
| 31   | [下一个排列](./0031_Next_Sequence.py)                 | 中等 | 广义双指针（类似字典序下一排列） |
| 42   | [接雨水](./0042_Trapping_Rain_Water.py)               | 苦难 | 双指针（动态规划）               |

---
*持续更新中，欢迎指点。*